rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {

    // DEV-ONLY rules for the Firebase Emulator (Linux/macOS/Windows local dev).
    // DO NOT DEPLOY THESE TO PRODUCTION.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
